config:    
    #
    # TRX MANAGER
    #
    trxManager:
        primary:
            datasource:
                hikari:
                    driverClassName: 'com.mysql.cj.jdbc.Driver'
                    jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_main_1?createDatabaseIfNotExist=true&autoReconnect=true'
                    username: '<USERNAME>'
                    password: '<PASSWORD>'
                agroal:
                    jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_main_1?createDatabaseIfNotExist=true&autoReconnect=true'
                    username: '<USERNAME>'
                    password: '<PASSWORD>'
                dbcp:
                    driverClassName: 'com.mysql.cj.jdbc.Driver'
                    jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_main_1?createDatabaseIfNotExist=true&autoReconnect=true'
                    username: '<USERNAME>'
                    password: '<PASSWORD>'
                basic:
                    driverClassName: 'com.mysql.cj.jdbc.Driver'
                    jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_main_1?createDatabaseIfNotExist=true&autoReconnect=true'
                    username: '<USERNAME>'
                    password: '<PASSWORD>'
            audit:
                datasource:
                    hikari:
                        driverClassName: 'com.mysql.cj.jdbc.Driver'
                        jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_audit_1?createDatabaseIfNotExist=true&autoReconnect=true'
                        username: '<USERNAME>'
                        password: '<PASSWORD>'

        secondary:
            datasource:
                hikari:
                    driverClassName: 'com.mysql.cj.jdbc.Driver'
                    jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_main_2?createDatabaseIfNotExist=true&autoReconnect=true'
                    username: '<USERNAME>'
                    password: '<PASSWORD>'
            audit:
                datasource:
                    hikari:
                        driverClassName: 'com.mysql.cj.jdbc.Driver'
                        jdbcUrl: 'jdbc:mysql://<HOST>:<PORT>/template_springboot_mvc_audit_2?createDatabaseIfNotExist=true&autoReconnect=true'
                        username: '<USERNAME>'
                        password: '<PASSWORD>'
